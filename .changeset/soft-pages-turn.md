---
"docs": "patch"
---

Installation, production, environment and troubleshooting rewritten for the single-container deployment: one port, one upstream behind a reverse proxy, `RUSTRAK_DASHBOARD` and `RUSTRAK_DASHBOARD_DIR`, and a "Dashboard on its own host" section for `rustrak-ui` with `RUSTRAK_API_URL`.
